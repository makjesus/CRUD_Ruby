# my_engine.gemspec
Gem::Specification.new do |spec|
    spec.name        = 'my_engine'
    spec.version     = '0.1.0'
    spec.summary     = 'MyEngine gem'
    spec.description = 'A description of MyEngine'
    spec.authors      = ['Your Name']
    spec.email       = ['your.email@example.com']
    spec.files       = Dir['lib/**/*.rb']
    spec.require_paths = ['lib']
  end
  